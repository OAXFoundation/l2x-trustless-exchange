#!/usr/bin/env bash
set -euo pipefail

REV="${1:-HEAD}"
FILES="$(git diff $REV --name-only -- '*.ts' 'src/*.js')"

if [ -z "$FILES" ]; then
    echo "No files to check."
else
    exec prettier --list-different $FILES
fi
